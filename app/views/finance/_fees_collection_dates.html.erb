

<%= select  :fees_submission, :dates_id, @dates.map { |e| [e.full_name, e.id]},
  {:prompt => t('app.views.finance.fees_collection_dates.select_fees_collection_date')},
  {:onChange => "#{remote_function( :url =>
    {:action => "load_fees_submission_batch"},
    :with => "'date='+value+'&batch_id='+$('fees_submission_batch_id').value") }"} %>


